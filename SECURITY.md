# 安全说明

BlockEcho 会处理网易登录会话、模型 API Key、SMTP 凭据和玩家反馈数据。请勿在公开 Issue、日志、截图或测试夹具中提交真实账号、密码、Cookie、Ticket、API Key、邮件授权码、服务器地址或数据库文件。

## 报告安全问题

请使用 GitHub 仓库的 Private vulnerability reporting 私下报告安全问题。报告中尽量提供可复现步骤和受影响版本，但请先替换所有真实凭据与个人信息。

## 部署要求

- 生产密钥必须通过环境变量或服务器私有配置提供，不得写入仓库。
- `MC_WEB_MASTER_KEY_FILE` 指向的主密钥必须独立于数据库保存，并限制文件权限。
- 网站必须通过 HTTPS 提供；模型 Base URL 和 SMTP 只能配置可信服务。
- 公开发布前应再次扫描 Git 历史，而不只是当前工作区。

如果凭据曾经进入 Git 历史，应立即撤销并轮换；仅从最新提交删除并不能消除泄露风险。
